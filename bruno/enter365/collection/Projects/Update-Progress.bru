meta {
  name: Update Progress
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/projects/:id/update-progress
  body: json
  auth: inherit
}

params:path {
  id: 1
}

body:json {
  {
    "progress_percentage": 50,
    "notes": "Halfway complete"
  }
}
