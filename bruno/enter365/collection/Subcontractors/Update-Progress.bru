meta {
  name: Update Progress
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/subcontractor-work-orders/:id/update-progress
  body: json
  auth: inherit
}

params:path {
  id: 1
}

body:json {
  {
    "completed_quantity": 50,
    "notes": "Halfway done"
  }
}
