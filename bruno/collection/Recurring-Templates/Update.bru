meta {
  name: Update Recurring Template
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/recurring-templates/:id
  body: json
  auth: inherit
}

params:path {
  id: 1
}

body:json {
  {
    "name": "Updated Template Name",
    "frequency": "quarterly"
  }
}
