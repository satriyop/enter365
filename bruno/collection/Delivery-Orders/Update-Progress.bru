meta {
  name: Update Progress
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/delivery-orders/:id/update-progress
  body: json
  auth: inherit
}

params:path {
  id: 1
}

body:json {
  {
    "status": "in_transit",
    "notes": "Package picked up by courier"
  }
}
