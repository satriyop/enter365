meta {
  name: Cancel Down Payment
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/down-payments/:id/cancel
  body: json
  auth: inherit
}

params:path {
  id: 1
}

body:json {
  {
    "reason": "Customer cancelled order"
  }
}
