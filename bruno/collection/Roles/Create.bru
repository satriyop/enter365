meta {
  name: Create Role
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/roles
  body: json
  auth: inherit
}

body:json {
  {
    "name": "manager",
    "display_name": "Manager",
    "description": "Manager role with limited permissions"
  }
}
