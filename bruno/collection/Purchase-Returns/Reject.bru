meta {
  name: Reject Purchase Return
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/purchase-returns/:id/reject
  body: json
  auth: inherit
}

params:path {
  id: 1
}

body:json {
  {
    "reason": "Not eligible for return"
  }
}
