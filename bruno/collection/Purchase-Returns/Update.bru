meta {
  name: Update Purchase Return
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/purchase-returns/:id
  body: json
  auth: inherit
}

params:path {
  id: 1
}

body:json {
  {
    "reason": "Updated reason"
  }
}
