meta {
  name: Cancel Purchase Return
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/purchase-returns/:id/cancel
  body: json
  auth: inherit
}

params:path {
  id: 1
}

body:json {
  {
    "reason": "No longer needed"
  }
}
